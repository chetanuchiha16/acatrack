---
name: Frontend CI

"on":
  push:
    branches: [main]
    paths:
      - "Student_Result_project/frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "Student_Result_project/frontend/**"
      - ".github/workflows/frontend-ci.yml"
  workflow_call:

defaults:
  run:
    working-directory: Student_Result_project/frontend

jobs:
  lint-typecheck-build:
    name: ESLint + TypeScript + Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: Student_Result_project/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint

      - name: TypeScript type-check
        run: npx tsc --noEmit

      - name: Vite build
        run: npm run build
